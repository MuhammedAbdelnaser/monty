#include "monty.h"

/**
 * nop: doesn’t do anything
 * @head: stack head
 * @line_number: line_number
 * Return: no return
 */

void nop(stack_t **head, unsigned int line_number)
{
  (void)head;
  (void)line_number;
}
